<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.toolsshopapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GrinderDetailActivity"></activity>
        <activity android:name=".GrinderCategoryActivity" />
        <activity android:name=".HammerDrillDetailActivity" />
        <activity android:name=".HammerDrillCategoryActivity" />
        <activity android:name=".DrillCategoryActivity" />
        <activity android:name=".DrillDetailActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"> <!-- только вертикальное положение -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>